# MiniShellAgent配置文件
# 复制此文件为 .env 并填入您的配置

# ==================== OpenAI配置 ====================
# OpenAI API Key (必需，如果使用OpenAI)
OPENAI_API_KEY=your_api_key_here

# OpenAI API Base URL (可选，默认使用官方API)
OPENAI_BASE_URL=https://api.openai.com/v1

# OpenAI 模型名称 (可选，默认 gpt-5-mini)
OPENAI_MODEL=gpt-5-mini


# ==================== 本地Llama配置 ====================
# Llama模型路径 (必需，如果使用本地Llama)
LLAMA_MODEL_PATH=/path/to/your/llama-model.gguf

# 上下文长度 (可选，默认 4096)
LLAMA_N_CTX=4096

# GPU加速层数 (可选，默认 0，设置为 -1 使用全部GPU)
LLAMA_N_GPU_LAYERS=0


# ==================== Agent配置 ====================
# 最大执行步数 (可选，默认 10)
MAX_STEPS=10

# 最大重试次数 (可选，默认 3)
MAX_RETRIES=3


# ==================== 安全配置 ====================
# 安全模式 (可选，默认 true，建议保持开启)
SAFE_MODE=true


# ==================== UI配置 ====================
# 详细输出 (可选，默认 false)
VERBOSE=false

